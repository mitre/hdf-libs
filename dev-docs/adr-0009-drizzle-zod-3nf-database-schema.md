# ADR-0009: 3NF Relational Schema for HDF (Drizzle + Zod + Go, PostgreSQL/PGlite)

**Status:** Proposed
**Date:** 2026-09-01 (revision 4, after three rounds of independent multi-lens design review)
**Deciders:** Aaron Lippold (author); Will Dower (maintainer review — see "Reviewer challenge points")

> **How to review (~40 min):** read "Reviewer challenge points" first — those are the decisions we want challenged. Then the Context bullets (identity reality, validator gap, Drizzle/runtime facts) → Decision §2, §3, §5, §8 → Alternatives B, E, G. The Implementation Plan is card-shaped input for the project-card skill; skim Scope, the prerequisite cards, Phase 0, Phase 1a, and Phase 7. Reply per challenge letter rather than per line. Cross-ADR links: §1 ↔ ADR-0008 §6 (packaging); Phase 6 ↔ ADR-0008 Phase 4 (count fixes); both "Delivery" lines.

## Context

HDF documents are JSON artifacts. Consumers that need to persist, index, and query them relationally — a future `hdf db` workflow in the CLI, the HDF MCP server (ADR-0007), ecosystem services building assessment stores, partner tooling — each invent tables today, or store whole documents as blobs and give up cross-document querying.

The motivating queries are about **adjudication reuse**:

1. **Carry-forward** — a new scan arrives; which of its findings were already adjudicated (waived, false-positive, risk-adjusted) by a prior scan's overrides or a standing amendments document for the same target?
2. **Similar-system precedent** — how did other systems applying the same baseline, with the same component kinds, adjudicate this control?
3. **Package scoping** — run either query scoped to one evidence package (an authorization context) or across packages within the caller's tenant.

These three queries are the acceptance spine: the relational shape is right only if each is a join, and each is a test with negatives.

The MCP server is a first-party consumer on both sides (it drafts amendments today and cannot see across documents), and it is **Go**, as is the CLI. This package is therefore dual-language from the start, following the repo's engine norm (shared-fixture cross-language parity, ADR-0007).

Realities the reviews established, which the schema is built around rather than assumed away:

- **Identity is not carried by the schema.** `Evaluated_Requirement` has no component reference; attribution exists only at baseline level via the `labels.component` convention and `hdf-system` target selectors — but `Standalone_Override.componentRef` and `inheritedFrom` exist (no shipped code honors them). `(requirement id, baseline name)` is not unique even within one document (a real fixture carries `SV-257777` twice in one baseline). `componentId` appears in 0 of 116 results fixtures, `systemRef` in 0 of 116 and in neither amendments fixture — real results reach a system only through evidence-package membership. `systemId` is optional (the two real OSCAL-derived system documents carry `identifier` + `identifierScheme` instead). "Baseline identity" spans three unrelated spaces (`Evaluated_Baseline.name`, system `baselineRefs`, plan `baselineRef` URI-references) and the corpus contains only exact-name matches. ADR-0005's change-event key is a different triple (`systemRef`, `componentId`, `requirementId`). `hdf amend apply` matches overrides on `requirementId` across all baselines unless `baselineRef` is set (exact name; absent means any baseline), ignores `componentRef`, and appends a copy of each applied standalone override into `statusOverrides` with no origin id.
- **Types the schema keeps distinct must stay distinct.** Inline `Status_Override` and standalone amendments overrides have *conflicting* validity rules. Inline `POAM` and `Standalone_Override{type: poam}` differ. `Checksum` ≠ `Integrity`. Five reference definitions exist; the schema's own `$comment` on `External_Evidence_Reference` says "Do not merge these". Root `labels` on amendments/system/evidence-package/plan is an unconstrained object; component and baseline `labels` are string maps. `tags` is arbitrary JSON and required. `Base_Component` has 13 kinds and is `additionalProperties: true`. Comparison `before`/`after` are full requirement snapshots; change-event `before` is an `Effective_Projection`, `after` a full requirement.
- **The validators do not enforce `unevaluatedProperties`** (draft-07 engines in both languages). Real fixtures carry root keys no schema defines (`targets`; the bundle results carry `platform`, `version`), and `hdf amend apply` writes a root-level `previousChecksum` onto results — and its verifier reads a root-level `previousChecksum` off the *amendments* document — neither location declared by any root schema. "Schema-valid" today means the validators' verdict, not conformance to the schema text.
- **Byte-hash integrity cannot survive re-serialization.** The shipped chain verifier hashes the *entire prior results document's bytes*; per-override `previousChecksum` has a producer (`hdf amend create`: sha256 of Go `json.Marshal` of the override) but no verifier. Signatures and evidence `Content_Reference.checksum` hash bytes. Canonical timestamp parsing truncates to milliseconds and converts offsets to UTC; 1,294 corpus `startTime` values carry microsecond precision.
- **Enums**: 44 `enum` sites, one a gating enum inside an `if`; `@mitre/hdf-schema` ships 41 generated runtime `enum` objects covering every closed enum (named and inline); the two open enums (`Bom_Type`, `External_Evidence_Format`) accept `x-` values; `Cloud_Provider` is nullable. HDF documents do **not** self-describe their schema version.
- **The fixture corpus is thin**: `hdf-fixtures` holds two results, one baseline, two amendments. The only real system/plan/evidence documents form one relative-URI *bundle* in `hdf-cli` testdata — whose `plan.json` carries an absolute machine-local `systemRef` pinned by the bundle's checksums. Two real system documents exist as `oscal-to-hdf` expected outputs. The only real scan × adjudication overlap is a converter-local twistlock output × `multi-cve-amendments.json` (CVE-2021-44228/45046/45105), and neither carries a `systemRef` or sits in a package.
- **Drizzle facts** (verified 2026-09-01): `drizzle-orm@latest` is 0.45.2; 1.0 is `1.0.0-rc.4` (rc; no GA). Only 1.x has `./zod` and the per-folder `migration.sql` + `snapshot.json` layout (`snapshot.json` is `version: '8'`, a flat `ddl` entity list, undocumented and without a stability promise). drizzle-kit has no down migrations. The 1.x migrator matches applied migrations by folder **`name`** (hash and `created_at` are recorded but not used); `hash` = sha256 of the byte-identical `migration.sql`; `created_at` = millis from the folder-name timestamp; `applied_at` defaults in the DB. A programmatic `migrate(migrations: MigrationMeta[], db, config)` exists in `drizzle-orm/pg-core` for non-filesystem consumers.
- **Runtime facts**: `@electric-sql/pglite@0.5.8` embeds **Postgres 18.3**; `embedded-postgres` v1.34 defaults to 18.3.0, downloads a native binary from Maven Central at first run, and exposes no checksum option (it does expose `CachePath`, `BinariesPath`, `Password`, `StartParameters`). Go cannot embed PGlite in-process (WASI build and `libpglite` are draft/do-not-use; community Go ports are non-functional) but can connect via the official `pglite-socket` server (dev-only, no SSL, one multiplexed connection, pinned in lockstep to PGlite). This repo's CI matrix is ubuntu + windows; PGlite's own CI runs Ubuntu only. The release workflow stamps every workspace package with the repo tag and only `next`-tags `hdf-converters`.

## Decision

### 1. Package, languages, layering
Create **`@mitre/hdf-db-schema`** (TypeScript: Drizzle `pg` tables, `drizzle-orm/zod` row schemas, pure `disassemble`/`assemble`, transactional `importDocument`/`exportDocument` as the primary API) and its Go module **`github.com/mitre/hdf-libs/hdf-db-schema/go`** — **no `/v3` suffix while the npm package is `0.x`** (a `v3` module path would require tags ≥ 3.0.0; the suffix is added when the package leaves `0.x`, the one deliberate departure from the sibling modules' path convention). Go row structs are **generated at build from drizzle-kit's `snapshot.json`** by a small generator that asserts `version === '8'` and fails loudly otherwise (drizzle-kit exact-pinned); column parity is tested. Import/export runs against `pgx`; migrations are applied from an embedded copy. Every phase delivers both languages with a shared-fixture parity test in the same commits. The module joins `go.work`, `hdf-cli`'s `replace` set, and the release workflow's `GO_MODULES`; the package ships a `test:go` script so the Windows matrix runs its Go tests. Layering: beside `hdf-engine` (depends on `hdf-schema`, `hdf-utilities`, `hdf-validators`, `hdf-fixtures` in tests); `hdf-utilities` gains no dependency. Packaging follows ADR-0008 §6.

### 2. Scope: core-first, all eight types committed, checkably
**v1 (core)** models `hdf-results`, `hdf-amendments`, `hdf-system`, `hdf-evidence-package`, and `hdf-baseline` in full 3NF. **Phase 7 of this ADR** adds `hdf-plan`, `hdf-comparison`, and `hdf-requirement-change-event`. Honest rationale: the exemplars need exactly the core set; comparison and change-event have no real corpus and carry snapshots needing a distinct model (`jsonb` + optional FK); a change-event table re-opens ADR-0005's external-reconciler decision. **Plan has a real corpus** (the promoted bundle) and is deferred for sequencing only — pulling it into core is cheap and is reviewer challenge (a). Mechanisms that keep the deferral honest: the package stays **`0.x` until all eight types round-trip** (independent of the `next` dist-tag, which lifts at Drizzle GA — the package may be `1.0` under `next` or `0.x` under `latest`); `documents` accepts all eight `doc_type`s from Phase 1a (bytes, hash, `validated_against`, `disassembled = false` for non-core types) and root-level **reference extraction runs for all eight regardless of `disassembled`**, so package membership and reference resolution work; `importDocument` on a non-core type returns `UNSUPPORTED_DOC_TYPE` for disassembly, asserted by a test; Phase 7 is sized with Files and a Verification command.

### 3. Identity model — from the schema outward
- Surrogate `uuid` primary keys; `ordinal` on every array child; uniqueness is `(parent_row_id, ordinal)`.
- **Requirement identity**: non-unique index on `(requirement_id, baseline_name)`; natural fields verbatim.
- **Attribution is derived, never document truth**: `baseline_component_attribution(baseline_row_id, component_row_id, method ∈ {labels.component, targetSelector, singleComponent, none})`, populated at import, never emitted by `assemble()`. `overrides.component_ref` is the natural column the schema provides; the carry-forward predicate honors it when present, and a component-scoped override can never match a target that has no `component_id` (schema-correct; stated, with a negative test). The ADR records that `hdf amend apply` currently ignores `componentRef`.
- **Component identity**: `components.component_id uuid NULL`; derived identity by `componentId` → system `targetSelector` over labels → `hostname`/`fqdn`/`name`, recorded in `component_identity_method`; never in a unique constraint. Intra-system references (`providedBy`, `inheritedBy`, data-flow endpoints) are validated at import into `unresolved_refs`, not FKs.
- **System identity**: `system_key` by `systemId` → `identifier` + `identifierScheme` → `name`, recorded with method; a derived `document_system_link(document_id, system_row_id, method ∈ {systemRef, packageMembership, manual})` gives every results/amendments document its system — for the real corpus that path is package membership (or `manual` for the standalone exemplar pair).
- **Baseline identity**: natural key `(name, version?)`; `baseline_aliases(alias, alias_kind ∈ {name, path, uri}, canonical_key, version)` populated by exact name plus an optional **curated alias input** — no fuzzy matching. A standalone override with **no `baselineRef` matches any baseline** (the `hdf amend apply` semantic), and the alias join must not drop such rows.
- **Document references**: `systemRef`, `planRef`, `Content_Reference.uri`, `derivation.seed.uri` are relative (or, in one real fixture, absolute machine-local) paths, not ids; `document_refs(from_document_id, field, raw_ref, resolved_document_id NULL, method ∈ {checksum, uriAtImport, manual}, algorithm)` resolves them — `checksum` resolution for sha256 uses `documents.source_sha256`; other `Hash_Algorithm`s compute from `source_bytes` and are unavailable when retention is disabled; unresolvable refs keep `raw_ref` with `resolved_document_id NULL`. `importDocument` takes an explicit import context (base directory / resolver).
- **Owner vs reference FKs**: every table that references two documents' rows (`document_refs`, `document_system_link`, `baseline_component_attribution`, `package_members`) declares exactly **one owner FK** (the importing side); all other FKs are **reference FKs** whose resolution at import is restricted to rows whose owning document's `organization_id IS NOT DISTINCT FROM` the importing document's. The ownership walker and RLS generator follow only owner FKs.
- **Adjudication provenance**: because `hdf amend apply` copies standalone overrides inline without an origin id, the exemplar spec dedups on `(type, requirement_id, applied_at, appliedBy.type, appliedBy.identifier)`, preferring the standalone row; `overrides.origin_override_id` is nullable, populated when the apply tool records provenance.
- **Reconciliation with ADR-0005**: results and amendments rows carry the baseline-qualified identity **and** a derived projection onto `(systemRef, componentId, requirementId)`; the mismatch is documented.
- All derived identity columns are nullable, indexed, and invisible to round-trip output.

### 4. Entity inventory — from the `$defs` graph
Phase 1a enumerates shared primitives from the `$defs` graph. Principles: one physical `overrides` table with `carrier ∈ {inline, standalone}` and carrier-conditioned CHECKs; `poams` (inline) separate from `overrides{type: poam}`; `checksums` separate from `integrity`; five reference tables, `Reference.ref` as `jsonb`; `labels` as EAV where the schema is a string map and `jsonb` for the unconstrained root form; `tags` as `jsonb` (GIN) plus a derived, rebuild-only `requirement_tag_values` projection for `nist`/`cci`; `components` once, 13 kinds via per-kind detail tables plus `extra jsonb` and an exactly-one-detail-row check; `boms` normalized per ADR-0001 (only `boms[].document` passthrough) with a polymorphic subject; `derivation` in core; snapshots as `jsonb` + optional FK in Phase 7; dead `target`/`platform` primitives not modeled; `identities` deduplicated on `(type, identifier, description)` **within one owning document only**. Families: identities/checksums/integrity/references; overrides/poams/evidence/milestones/signatures/verification_methods; cvss/epss/kev/affected_packages/remediation; inputs/supported_platforms/dependencies/generator/tool; components/boms; labels/descriptions. **Owned tables are created together with their parent** (results-scoped primitives in Phases 2a–2c, amendments-scoped in 3, system-scoped in 4); the Phase 1b family cards deliver the shared column builders, Zod fragments, CHECK expressions, and tenant-free reference-data tables, so the ownership walker is green at every phase.

### 5. Lossless round-trip contract — concrete rules
`assemble(disassemble(doc))` is deep-equal to `canonicalize(doc)` **and** conforms to the schema text, over an enumerated corpus per type. Canonicalization is the stated lossy step: timestamps parsed by schema path with the repo's `parseTimestamp`/`hdfutil.ParseTimestamp` (**truncated to milliseconds**, offsets to UTC; never a regex sweep; never inside passthrough `jsonb`), Go decoding with `UseNumber`, both languages compared on canonical forms. Rules: `ordinal` on every array; **tri-state presence** via a `presence jsonb` column per parent row recording empty-present optional collections and explicit-null fields; `double precision` for scores/impacts, `bigint` for large counts, `date` for the three `format: date` fields, `timestamptz` for instants; NUL bytes (` `, legal in scanner output, rejected by Postgres text **and** `jsonb`) escaped by a documented reversible token in both scopes, both directions, with a planted fixture; `jsonb` passthrough is value-lossless only after canonical parse; `extra jsonb` overflow on every `additionalProperties: true` object; unions kept `jsonb` unless a variant column is lossless; TS `assemble` builds objects prototype-free (`Object.create(null)`) with a planted `__proto__` key. **Documents are immutable after import** (an `UPDATE` on an owned table is rejected by trigger and test); replacement is a new import.

**Strictness and legacy keys.** `disassemble` is **strict** — unknown keys outside `additionalProperties: true` scopes are rejected — except an **enumerated, versioned legacy-key list** (`targets`, `platform`, `version` at the results root; root `previousChecksum` on results and amendments) quarantined into `documents.extra` with provenance and claimed by the coverage test. The list is permanent import policy for documents produced by older tools. Two prerequisite cards fix the root causes: the validators move to 2020-12 engines in both languages, and `hdf amend apply`/`verify` record and read the chain digest at a schema-defined location (schema-ADR-sized if a field is needed). "Newer-minor" detection is exactly strict-disassemble unknown-key rejection, with `documents.validated_against` as provenance. A **schema-path coverage test** asserts every property path is claimed by a column, a `jsonb` scope, or the legacy list, and a **`$id` version-pin test** fails on a bump.

### 6. Integrity semantics
Stored `integrity`, `checksum`, `signature`, and `previousChecksum` values are preserved **verbatim, never recomputed**. `documents.source_sha256` records the ingested bytes; `documents.source_bytes` (`bytea`) is retained **by default** (disable per import) and obeys the same tenancy predicates as its document. The document-level chain verifies **only via `source_bytes` of the prior results document**; the per-override chain has a producer but no verifier and is out of scope until the schema specifies the hash input. `exportDocument` returns `{form: 'original' | 'assembled', verifiable: [...]}`; `require: 'original'` errors rather than silently falling back. Row-level tamper-evidence is out of scope. Rows are the value-level truth for querying; bytes are canonical for byte-level verification and export (reviewer challenge (c)).

### 7. Enums and versions
Every **closed** enum — named or inline — consumes the corresponding generated runtime enum from `@mitre/hdf-schema` (41 objects); a build-time walker handles only the two **open** enums with `CHECK (value IN (...) OR value ~ '^x-[a-z0-9]+(-[a-z0-9]+)*$')`. Closed → `text` + `CHECK IN (...)`; nullable → nullable column. The walker asserts the arithmetic **44 sites − 1 gating = 43 = 41 generated + 2 open**. Native `pg` enums are rejected for removability and transactional DDL; adding a value still requires a migration.

### 8. Tenancy seam — kept, with the leak analysis applied
One anchor: **`documents`** carries nullable, indexed `organization_id`, `team_id`, `created_by` (`text`; the first two follow better-auth's organization plugin names; `createdBy` is this package's convention). `systems` and `evidence_packages` are owned rows with a NOT NULL FK to their document and no tenancy columns. No auth tables, no FKs into auth. Rules: every table is **owned** (a NOT NULL owner-FK chain to exactly one `documents` row) or **reference data** (tenant-free); tenant-bearing primitives are never deduplicated across documents; the ownership walker fails on any tenant-bearing table without a single owner path and on a cross-tenant reference resolution (planted); `NULL` organization means **unowned, never global**; the predicate is `organization_id IS NOT DISTINCT FROM $1`, so a NULL caller sees only unowned rows (negative test on every exemplar); `team_id` is an optional sub-scope that never widens; tenancy is **never derived from document content** — `importDocument` takes an explicit ownership argument; "across packages" means within the caller's tenant; RLS ships as an **optional migration generated from the ownership walker** (one policy per owned table via its owner-FK path), tenant context via `SET LOCAL hdf.organization_id`, tested as a non-owner role under `FORCE ROW LEVEL SECURITY`, documented as migrations-run-as-owner / runtime-as-non-owner, and as a test aid only on PGlite. Driver errors are wrapped to strip `DETAIL`/`HINT`; query-parameter logging is asserted off; a canary test plants a sentinel value and asserts it never appears in any error or log in either language.

### 9. Migrations, drivers, dependencies
Drizzle **1.0.0-rc.4** (rc; GA not released as of 2026-09-01): peer `drizzle-orm >=1.0.0-rc.4 <2`, `zod ^3.25 || ^4` (zod-v4 API), `@electric-sql/pglite` and `pg` optional peers, `drizzle-kit` dev-only and exact-pinned (test-asserted). The package publishes under the **`next` dist-tag** until GA — which requires the release workflow to gain a per-package dist-tag policy and to exclude this package from the tag-version stamp while it is `0.x` (Phase 6). Migrations are forward-only from drizzle-kit; **reversibility is a package convention** — hand-authored `down.sql` per folder, tested up/down. The migrations directory is in `files`, its path exported as a constant, and a `./migrations` subpath exports drizzle's **`MigrationMeta[]`** (`{name, sql: string[] split on statement-breakpoint, folderMillis, hash, bps: true}`) so non-filesystem consumers call `migrate()` from `drizzle-orm/pg-core` and land in the same ledger. The build syncs `migration.sql`/`down.sql` byte-for-byte into `go/migrations/` (embed.FS; drift test); `.gitattributes` pins `eol=lf` on both trees. **Ledger parity**: the Go applier writes drizzle-compatible rows — `drizzle.__drizzle_migrations` with the same `name`, `hash` (sha256 of the byte-identical file), and `created_at` (folder millis), leaving `applied_at` to the DB default — and a test proves a database migrated by either language is accepted unchanged by the other, pinned to the exact rc. CI: TS on PGlite, Go on `embedded-postgres` pinned to `V18` (18.3.0) — **our CI step** downloads the Maven archive, verifies its sha256 against a tracked constant, caches it, and hands it over via `CachePath`; loopback via `StartParameters{listen_addresses: 127.0.0.1}`, random `Password()` — both on **ubuntu + windows**. Both engines' `SELECT version()` are goldens asserting the same major. `pglite-socket` is the documented local-dev bridge for Go: bind `127.0.0.1`, `sslmode=disable`, single connection, lockstep-pinned, with a Phase 6 verification line for pgx.

### 10. Zod's role
Row schemas via `drizzle-orm/zod`; cross-column gates as `superRefine` on assembled object schemas. Whole-document validation at the boundary uses `@mitre/hdf-validators` in **both** languages before `disassemble`.

## Reviewer challenge points

Will's review — and any agent review he runs — should specifically challenge or verify: (a) **core-first** (§2), including whether `hdf-plan` should be pulled into core given it has a real corpus; (b) **dual TS + Go now** (§1) at the honest estimate below; (c) **source bytes retained by default** and the rows-for-query / bytes-for-verification split (§6); (d) the **tenancy seam** (§8); (e) the identity derivation rules (§3), especially whether `labels.component` attribution should become a schema change; (f) the **legacy-key policy** (§5) and the two root-cause cards; (g) shipping on a **Drizzle rc under `next`** and the `0.x`/`next` gate interaction; (h) **Go structs generated from drizzle's undocumented `snapshot.json`** (§1); (i) **documents immutable after import** (§5); (j) the Go module path **without `/v3`** while `0.x`.

## Alternatives Considered

### A. jsonb document store with SQL views
Why rejected: not 3NF; no FK integrity; relational ergonomics is the point.

### B. Hybrid — 3NF rows plus the original document
Disposition: **partially adopted** — rows are value-level truth, bytes retained by default for verification/export, documents immutable (§5/§6). The revision-1 rejection was a strawman.

### C. Mechanically generate tables from JSON Schema
Why rejected: document-shaped nesting, no key design; sync enforced by coverage + round-trip tests instead.

### D. Other ORMs / raw SQL
Why rejected: Drizzle is TS-first, SQL-transparent, PGlite-native; its migrations are plain SQL the Go side consumes.

### E. TS-only v1, Go deferred
Why rejected: the motivating consumer cannot write a row — challenge (b).

### F. All eight types in v1
Why rejected: comparison/change-event have no corpus and carry snapshots; change events re-open ADR-0005 — challenge (a).

### G. Gate on Drizzle 1.0 GA, or target 0.45.x
Why rejected: unknown wait, or a forced layout rewrite; `next` on the rc with an exact-pinned parity test — challenge (g).

### H. Hand-written Go structs, or sqlc
Why rejected: hand mirrors drift; sqlc needs a query surface this package does not ship — challenge (h).

### I. Ship auth/tenancy tables
Why rejected: a seam, not a subsystem; no-ownership also rejected — challenge (d).

### J. Do nothing
Why rejected: no public issue asks for this today (anticipated by the `hdf db`/MCP direction); design cost is paid once.

## Consequences

Easier: adjudication carry-forward and similar-system precedent become joins; `hdf db` and a DB-backed MCP mode have a schema, migrations, validators; PGlite serves embedded TS consumers; the validator fix makes "schema-valid" mean conformance repo-wide.

Harder: every schema field addition forces a db-schema update; migrations and `down.sql` are a public surface; dual-language doubles implementation; source-byte retention costs storage; an rc dependency until GA; two release-workflow changes.

Risks and mitigations: normalization misjudged late → core first, `importDocument`/`exportDocument` API, bytes allow re-projection; canonicalization ambiguity → §5 rules are tests; PGlite gaps or Windows breakage → Phase 1a AC on both CI OSes with the stated fallback (TS on embedded-postgres via `pg`); corpus thin → Phase 0 promotion under the fixture policy (§ Shared Abstractions); tenant leakage → §8 classification, owner/reference FK rule, generated RLS, negative tests; ledger divergence → parity test; data sensitivity → canary test, driver-error wrapping, docs stating PGlite persists plaintext and encryption at rest is the deployer's; fixture hygiene → `lint:fixtures` (IPv4/IPv6 outside documentation ranges, non-RFC-2606 FQDNs, non-`example.*` emails, cloud ids, key-shaped strings, **absolute local filesystem paths** — with the bundle's `plan.json` as the recorded exception, since its bytes are checksum-pinned) over goldens and docs, and `hdf-fixtures/README.md` gains a `sanitized (yes/no/n-a)` column; supply chain → PGlite and embedded-postgres versions pinned and goldened, archive checksummed by our step, no `minimumReleaseAgeExclude`.

## Implementation Plan

### Scope
IN: the TS package and Go module; core-first 3NF; identity/attribution/alias/link/reference tables with owner/reference FKs; round-trip, coverage, `$id`-pin, ownership, canary tests; integrity semantics; enum handling; tenancy seam with generated RLS; drizzle rc under `next`; Go struct generator, migrations embed, pgx applier, ledger parity; PGlite + embedded-postgres CI on ubuntu + windows; docs with runnable walkthroughs; the two root-cause prerequisite cards; the release-workflow changes.
OUT: any server; `hdf db` CLI commands and the Go local-database decision (next ADR); auth itself; row-level tamper-evidence; query helpers beyond import/export; analytics marts.
Delivery: **one epic on one sequential branch** (every phase touches the shared schema/migrations tree); both ADRs land together in the design PR first.

### Quality Standards (inherited by every card)
- PGlite (TS) and embedded-postgres pinned 18.3 (Go) on ubuntu + windows; version goldens agree on major.
- Every migration has a tested `down.sql`; Go/TS ledger parity on the exact rc; migrations byte-synced, `eol=lf`.
- Round-trip over the enumerated per-type corpus + schema examples: deep-equal to `canonicalize(doc)` and conformant to the schema text; strict `disassemble` with the legacy list; coverage and `$id`-pin green; documents immutable.
- Absent is NULL, never fabricated; tri-state presence preserved; typing per §5; NUL and prototype-key fixtures planted.
- Enum arithmetic 44 − 1 = 43 = 41 + 2 asserted.
- Ownership walker green (owner-FK rule; planted cross-tenant resolution fails); tenancy never from content; NULL-caller and cross-tenant negatives on every exemplar; canary test green.
- Boundary validation with `hdf-validators` in both languages; no content in errors/logs.
- Fixtures per the repo policy: real tool runs promoted into `hdf-fixtures` (both consumers import them, originals deleted); any derived fixture lives in `hdf-db-schema/test/fixtures/derived/` with its checked-in transformation script and a logged 2020-12 validation, never in `hdf-fixtures`.
- TDD, >90% coverage, zero lint warnings, three-artifact rule, `publint`/`attw`, repo gauntlet; peer layout per §9.
- No commercial downstream consumers referenced anywhere.

### Shared Abstractions
| Shared need | Used by | Card it as |
|---|---|---|
| Corpus, per the fixture policy: promote the `hdf-cli` evidence bundle **intact** (`hdf-fixtures/evidence-package/rhel9-bundle/`), the two real `oscal-to-hdf` system documents, and the twistlock T1 (`twistlock-twistcli-coderepo-scan-sample.json.hdf.json`, repointing the converter test) — originals deleted, README provenance + `sanitized` column; **T2 produced by a real tool run** (`hdf amend apply T1 multi-cve-amendments.json` from a checked-in script, its output committed as a derived fixture with logged validation); no cross-package glob | all | Phase 0 |
| `documents` anchor (all eight `doc_type`s; tenancy; `source_sha256`/`source_bytes`; `validated_against`; `disassembled`; `extra` with legacy keys), `document_refs`, `document_system_link`, `baseline_aliases`, `baseline_component_attribution`, owner/reference FK annotations, ownership walker | all | Phase 1a |
| Shared column builders, Zod fragments, CHECK expressions, reference-data tables by family (§4) | 2–5 | Phases 1b-i/ii/iii |
| Harness: canonicalize by path, presence, deep-equal, 2020-12 conformance, corpus runner, coverage walker, `$id` pin, enum arithmetic, canary, PGlite + embedded-postgres runners, migration up/down + ledger parity, Go struct generator + column parity | all | Phase 1c |
| Exemplar-query definitions: exact join keys, `as_of` window (`applied_at <= as_of < expires_at`, NULL expiry = open), NULL-`baseline_ref` wildcard, dedup rule, `applyCompatible` mode, system-link method, negatives | 3–5 | Phase 1a (spec), proven 3–5 |

### Phases (TS + Go + parity each; sp = agent-pace; "Depends on" feeds `bd dep add`)

**Prerequisite cards (outside this package).** **Validators to 2020-12 engines — sp8; depends on: nothing.** Files: `hdf-validators/typescript/index.ts` (ajv 2020), `hdf-validators/go/validators.go` (a 2020-12 Go validator), tests, fixture fallout. AC: [ ] an extra key under `unevaluatedProperties: false` fails validation in both languages; [ ] all schema examples still pass; [ ] fallout in fixtures/converters fixed at root cause. Verification: `cd hdf-validators && pnpm test && go test ./go/...` and `pnpm check`. **Chain digest to a schema-defined location — sp5; depends on: nothing (schema-ADR-sized if a field is needed).** Files: `hdf-diff/go/amend/amend.go` (writer and reader), TS twin, schema change if required. AC: [ ] `amend apply`/`verify` no longer read or write undeclared root keys; [ ] existing chain fixtures verify. Verification: `cd hdf-diff && go test ./go/... && pnpm test`.

**Phase 0 — Corpus (sp5; depends on: nothing).** Files: `hdf-fixtures/evidence-package/rhel9-bundle/*`, `hdf-fixtures/system/{ssp-example,ssp-fedramp}.json`, `hdf-fixtures/results/twistlock-coderepo-scan.json`, `hdf-fixtures/README.md`, `hdf-fixtures/src/index.ts`, `hdf-fixtures/fixtures.go`, `hdf-fixtures/package.json` (`files`); originals deleted from `hdf-cli/cmd/hdf/cmd/testdata/evidence-verify/`, `hdf-converters/converters/oscal-to-hdf/fixtures/expected/`, `hdf-converters/converters/twistlock-to-hdf/fixtures/expected/` with their tests repointed; `hdf-db-schema/test/fixtures/derived/{t2-applied.json,README.md}` + `scripts/derive-t2.mjs`. AC: [ ] bundle promoted intact and indexed as a set; [ ] all promoted files have provenance and a `sanitized` value; [ ] every former consumer imports from `hdf-fixtures`; [ ] T2 derived by the real `hdf amend apply` run, validated with a 2020-12 engine, proof logged. Verification: `pnpm check`.

**Phase 1a — Scaffold, anchor, identity, ownership walker (sp13; depends on: 0).** Files: `hdf-db-schema/{package.json,tsconfig.json,tsdown.config.ts,eslint.config.js,drizzle.config.ts}`, `src/schema/documents.ts`, `src/schema/identity.ts`, `src/import-context.ts`, `src/ownership.ts`, `migrations/0000_*/{migration.sql,snapshot.json,down.sql}`, `go/{go.mod,doc.go,migrate.go}`, `go/internal/gen/` (struct generator), `go/migrations/`, `scripts/{sync-go-migrations,gen-go-structs}.mjs`, `.gitattributes`, `go.work`, `hdf-cli/go.mod`, tests (TS + Go). AC: [ ] migrations up/down on PGlite in-memory on ubuntu **and** windows, and on embedded-postgres 18.3 (fallback if PGlite fails on windows: TS suite via `pg` there, stated in the card); [ ] ledger parity (`name`/`hash`/`created_at`, `applied_at` defaulted) both directions on the exact rc; [ ] generated Go structs column-parity with `snapshot.json`, generator refuses any other snapshot version; [ ] tenancy columns nullable, indexed, invisible to round-trip; [ ] `documents` accepts all eight types; non-core disassembly returns `UNSUPPORTED_DOC_TYPE`; root-level references extracted for all eight; [ ] ownership walker fails on a planted un-anchored table and on a planted cross-tenant reference resolution. Verification: `cd hdf-db-schema && pnpm test && cd go && go test ./...`.

**Phases 1b-i/ii/iii — Shared families (sp8 each; depends on: 1a).** Files: `src/schema/primitives/{identity,references}/*.ts` (1b-i), `src/schema/primitives/{overrides,poams,evidence,milestones,signatures}/*.ts` (1b-ii), `src/schema/primitives/{cvss,epss,kev,affected_packages,inputs,platforms,dependencies,tooling}/*.ts` (1b-iii), matching `go/primitives/<family>.go`, reference-data migrations, tests. AC (each): [ ] every primitive in the family has a column builder / Zod fragment / CHECK expression or a documented `jsonb` disposition; [ ] 1b-ii: the carrier-conditioned override CHECK expression accepts the results example and rejects the standalone-invalid case (unit-tested on the expression); [ ] reference-data tables tenant-free per the walker. Verification: as 1a.

**Phase 1c — Harnesses (sp13; depends on: 1a).** Files: `test/harness/*.ts`, `go/internal/harness/*`, `test/fixtures/{nul,proto}.json`. AC: [ ] each planted failure detected (order change, dropped empty array, unknown key, legacy key accepted only from the list, missing column claim, cross-tenant primitive, enum arithmetic drift, `$id` drift, canary leak, `__proto__` key, NUL token). Verification: as 1a.

**Phase 2a — Results: baselines + requirement core (sp13; depends on: 1b-i, 1c).** Files: `src/schema/results/{baselines,requirements,descriptions,references,tags,labels}.ts`, `src/results/{disassemble,assemble}.ts`, `go/results/{schema,disassemble,assemble}.go`, migration. AC: [ ] corpus results round-trip with the rest temporarily in overflow; [ ] duplicate requirement ids preserved by ordinal; [ ] legacy root keys quarantined with provenance; [ ] ownership walker green (owned tables created with their parent). Verification: as 1a.

**Phase 2b — Results: results, overrides, POA&Ms, evidence, vulnerability fields (sp13; depends on: 2a, 1b-ii, 1b-iii).** Files: `src/schema/results/{results,overrides,poams,evidence,cvss,epss,kev,affected_packages}.ts`, `go/results/*.go`, migration. AC: [ ] full corpus results round-trip; [ ] chain test: T1 `source_sha256` equals T2's root `previousChecksum`; assembled output reported non-verifiable. Verification: as 1a.

**Phase 2c-i — Components (13 kinds) + boms (sp13; depends on: 2b).** Files: `src/schema/components/{base,kinds/*,boms}.ts`, `go/components/*.go`, migration. AC: [ ] every component in the corpus round-trips with kind-specific columns and `extra` only for genuinely extra keys; [ ] exactly-one-detail-row check enforced. Verification: as 1a.

**Phase 2c-ii — Inputs, statistics, transactional import/export (sp8; depends on: 2c-i).** Files: `src/schema/results/{inputs,statistics}.ts`, `src/import-export.ts`, `go/importexport.go`. AC: [ ] full results corpus green, no overflow outside `additionalProperties: true` scopes; [ ] coverage green for hdf-results; [ ] `importDocument`/`exportDocument` transactional on both drivers, both languages; export shape discriminated; immutability trigger tested. Verification: as 1a, plus `pnpm check`.

**Phase 3 — Amendments + carry-forward exemplar (sp13; depends on: 2c-ii).** Files: `src/schema/amendments/*.ts`, `src/amendments/{disassemble,assemble}.ts`, `go/amendments/*.go`, `test/exemplars/carry-forward.test.ts`, `go/exemplars_test.go`. AC: [ ] amendments corpus round-trips; [ ] carry-forward: for a T2 failed finding, the documented query with explicit `as_of` returns prior adjudications (inline or standalone, deduplicated by §3) whose `(requirement_id, canonical baseline)` match — treating absent `baseline_ref` as any baseline — and whose system link matches (`manual` for the T1/T2 pair; re-proven via `packageMembership` in Phase 5), honoring `component_ref` when present; [ ] `applyCompatible` mode restricts to exact baseline-name matching and excludes alias-only matches; [ ] negatives: different canonical baseline; component-scoped override vs un-identified target; other tenant; NULL caller. Verification: as 1a.

**Phase 4 — System + similar-systems exemplar (sp13; depends on: 3).** Files: `src/schema/system/{systems,boundaries,dataflows,designations,unresolved_refs}.ts`, `go/system/*.go`, `test/exemplars/similar-systems.test.ts` + Go. AC: [ ] promoted system corpus (bundle system + the two OSCAL-derived systems, exercising `system_key` rules 1 and 2) round-trips; [ ] component kinds queryable; [ ] similar-systems: an adjudication on system A surfaces as precedent for the matching control on system B sharing a canonical baseline and overlapping kinds, within tenant; negatives: other tenant, non-overlapping kinds. Verification: as 1a.

**Phase 5 — Evidence package, package scoping, RLS (sp13; depends on: 4).** Files: `src/schema/evidence_package/{packages,members}.ts`, `go/evidence/*.go`, `migrations/optional/rls/{up.sql,down.sql}` (generated), `scripts/gen-rls.mjs`, `test/exemplars/package-scope.test.ts` + Go, `test/rls.test.ts`. AC: [ ] bundle corpus round-trips; every `contents[]` member resolves by checksum; `plan.json`'s absolute `systemRef` lands as `raw_ref` with `resolved_document_id NULL`; [ ] package-scoping exemplar bounded correctly within and across packages in-tenant, and the T1/T2 carry-forward re-proven with `packageMembership` via a derived package (derived-fixture rules apply); [ ] RLS generated for every owned table, applied on PGlite with `FORCE` + non-owner role: tenant B invisible on every table, NULL-tenant rows invisible to scoped roles; a table added without a policy fails the generator test. Verification: as 1a.

**Phase 6 — Docs, walkthroughs, release/CI wiring (sp8; depends on: 5).** Files: `site/docs/guides/hdf-db-schema.md`, `hdf-db-schema/examples/{quickstart.test.ts,quickstart_example_test.go}` (executed in CI), `scripts/check-fixture-hygiene.mjs` + root `lint:fixtures`, `.claude/commands/release.md`, `.github/workflows/release.yml` (exclude this package from the tag-version stamp while `0.x`; per-package dist-tag policy adding `next` for this package; `GO_MODULES`), `.github/workflows/{ci,pre-release}.yml` (build verify loop, artifact paths, `verify-packages` and publish `PACKAGES`, pre-release smoke-import list, coverage Go dir list — adding every missing Go module: `hdf-engine/go`, `hdf-diff/go`, `hdf-generators/go`, `hdf-utilities/go`, `hdf-extension-graph/go`; embedded-postgres cache step), root `package.json` (`lint:go`), memory `hdf-schema-change-propagation` step 5, `CLAUDE.md` counts (only if ADR-0008 Phase 4 has not landed). AC: [ ] examples execute on both drivers; [ ] pgx-over-pglite-socket verification line recorded; [ ] release workflow publishes this package under `next` and leaves its `0.x` version untouched (dry-run evidence); [ ] every enumeration includes the package; [ ] hygiene lint green. Verification: `cd site && pnpm generate && pnpm build`, `pnpm check`.

**Phase 7 — plan, comparison, change events (sp21 — split at cut time; depends on: 5; cards cut after Phase 5 closes).** Files: `src/schema/{plan,comparison,events}/*.ts`, `src/{plan,comparison,events}/{disassemble,assemble}.ts`, `go/{plan,comparison,events}/*.go`, migrations, corpus promotion for comparison and change-event (real documents required; the bundle's plan is already promoted). AC: [ ] all eight types round-trip and `disassembled = true` for each; [ ] the ADR-0005 reconciliation for the change-event append shape is decided in writing before implementation; [ ] `UNSUPPORTED_DOC_TYPE` test removed; package leaves `0.x`. Verification: `cd hdf-db-schema && pnpm test && cd go && go test ./...` and `pnpm check`.

**Totals**: Phases 0–6 ≈ sp149, Phase 7 ≈ sp21, prerequisites sp13 → ≈ sp183, roughly 24–32 h agent-pace at the repo's calibration (dual-language ≈ 1.8× single). Reviewer challenge (b).

### Verification Strategy
The per-type corpus round-trip suite (PGlite for TS, embedded-postgres for Go, shared fixtures, canonical forms compared) is the spine; each phase closes only when its types round-trip and conform, coverage/`$id`/ownership/canary tests are green, and its exemplar with negatives passes in both languages. Migrations tested up/down and ledger-parity each phase. Live test before each close: import a real fixture through both drivers in both languages and run the phase's exemplar query — output pasted in card notes.

## References
- PGlite README/docs; changelog (0.5.0 → Postgres 18.3) — https://github.com/electric-sql/pglite, https://pglite.dev/docs/about; PGlite Socket — https://pglite.dev/docs/pglite-socket (npm 0.2.11); v0.4 announcement — https://electric.ax/blog/2026/03/25/announcing-pglite-v04; pglite-bindings / pglite-build (draft, do-not-use); issue #89 (accessed 2026-09-01)
- Drizzle ORM 1.0.0-rc.4 / drizzle-kit 1.0.0-rc.4; migrator source (`name`-based matching; `MigrationMeta`; `migrate()` in `pg-core`); `snapshot.json` v8 — https://orm.drizzle.team, https://github.com/drizzle-team/drizzle-orm (accessed 2026-09-01); down-migration requests #4005, #5067
- better-auth 1.7.x organization plugin — https://www.better-auth.com/docs/plugins/organization (accessed 2026-09-01)
- embedded-postgres v1.34 (`V18` = 18.3.0; `CachePath`/`BinariesPath`/`Password`/`StartParameters`; no checksum option) — https://github.com/fergusstrange/embedded-postgres (accessed 2026-09-01)
- pgx — https://github.com/jackc/pgx (accessed 2026-09-01)
- This repo: ADR-0001, ADR-0005, ADR-0007, ADR-0008 §6; `hdf-diff/go/amend/amend.go`; `hdf-cli/cmd/hdf/cmd/amend_headless.go`; `hdf-validators/{typescript,go}`; `hdf-utilities` timestamp helpers; `.github/workflows/release.yml`; `CLAUDE.md` fixture-boundary rule; `hdf-fixtures/README.md`; memory `hdf-results-no-requirement-componentid`
