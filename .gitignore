# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
# Exception: Go generated types must be committed for go get to work
# (standard practice for generated Go source — see protobuf, sqlc, ent)
!hdf-schema/dist/go/
lib/
build/
*.tsbuildinfo

# Testing
coverage/
coverage.out
.nyc_output/
test-output/
.stryker-tmp/

# Environment variables
.env
.env.local
.env.*.local

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp
.cache/

# Generated files
*.log
hdf-cli/hdf

# VitePress schema site — generated at build time
site/schemas/*.md
site/public/schemas/

# Claude Code — track commands and project rules, but not local machine settings
.claude/settings.local.json

# Local research notes (to be converted to GitHub wiki pages)
hdf-cli/SAF_CLI_COMPARISON.md

# Beads local session state (not for public consumption)
.beads/recovery-context.md
.beads/recovery-prompt.md
.beads/archive/
.beads/push-state.json
.beads/dolt-monitor.pid.lock
.beads/.beads-credential-key
.beads/backup/

# bv (beads viewer) local config and caches
.bv/

# Dolt database files (added by bd init)
.dolt/
*.db
