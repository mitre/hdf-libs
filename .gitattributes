# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# Enforce LF line endings for all test fixtures and generated output so that
# checksums, snapshot comparisons, and diff-based assertions are identical on
# Windows (core.autocrlf=true) and *nix.  This is the industry-standard
# approach (Prettier, Hugo, ESLint, etc.).
hdf-converters/**/fixtures/** eol=lf
hdf-cli/cmd/hdf/cmd/testdata/**/*.json eol=lf
hdf-schema/test/**/*.json eol=lf
hdf-generators/test/**/*.json eol=lf
hdf-validators/test/**/*.json eol=lf

# Large binary fixtures tracked via Git LFS (must come after eol rules so
# -text overrides eol=lf for these specific files).
hdf-converters/**/fixtures/input/*.xml filter=lfs diff=lfs merge=lfs -text
hdf-converters/converters/oscal-to-hdf/fixtures/input/*.json filter=lfs diff=lfs merge=lfs -text
hdf-converters/converters/cyclonedx-to-hdf/fixtures/input/juice-shop-sbom.json filter=lfs diff=lfs merge=lfs -text
hdf-generators/test/fixtures/*.xml filter=lfs diff=lfs merge=lfs -text
