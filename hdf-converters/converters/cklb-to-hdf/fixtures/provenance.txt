# cklb-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `all-passing.cklb`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/issue 80 converter validity (#81)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `firefox-stig.cklb`
No counterpart in either upstream checked. Introduced by this repository in
`feat: add ckl-to-hdf converter (DISA STIG Viewer checklist) (#71)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/cklb-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `all-passing.cklb` — STIG Viewer CKLB; cklb_version (format version); stigs[0].stig_id=MOZ_Firefox_STIG version 6; STIG Viewer's own version is not in the file.
- `firefox-stig.cklb` — STIG Viewer CKLB; cklb_version (format version); stigs[0].stig_id=MOZ_Firefox_STIG version 6; STIG Viewer's own version is not in the file.

## Schema

DISA publishes no schema for CKLB that was located; STIG Viewer's distribution was not reachable from here to check (2026-09-20).
