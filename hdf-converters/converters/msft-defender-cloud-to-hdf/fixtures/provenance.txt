# Microsoft Defender for Cloud fixtures — provenance

**Schema-validated synthetic** (fixture-integrity option 3: no real tool output
available, so constructed and validated against the official schema). Each fixture
carries an inline `_provenance` field stating this; the summary is surfaced here
at the directory level.

- `sample.json` / `minimal.json` — constructed from the Microsoft Azure REST API
  documentation (`Microsoft.Security/assessments`, api-version 2021-06-01) and
  validated against the official Swagger spec at
  `github.com/Azure/azure-rest-api-specs`. Placeholder subscription/tenant GUIDs
  (`a1b2c3d4-…`) are intentional. **Not real tool output.**
- `empty.json` — zero-assessments document.

**Refreshing:** requires an Azure tenant with Defender for Cloud (licensed). To
replace with a real export, pull `Microsoft.Security/assessments`, scrub tenant/
subscription/resource IDs, and drop the `_provenance` marker.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.json` — Microsoft Defender API response body; the service version is not part of the response, so its version is not determinable from file content.
- `minimal.json` — Microsoft Defender API response body; the service version is not part of the response, so its version is not determinable from file content.
- `sample.json` — Microsoft Defender API response body; the service version is not part of the response, so its version is not determinable from file content.

## Schema

The fixture is an Azure Resource Manager `assessments` response; Microsoft publishes the Swagger 2.0 description in Azure/azure-rest-api-specs under `specification/security/resource-manager/Microsoft.Security/Security/stable/<api-version>/assessments.json`. It describes the endpoint, not a document format, and is not vendored (2026-09-20).
