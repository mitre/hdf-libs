# netsparker-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty.xml`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `sample-netsparker-invicti.xml`
Same filename as heimdall2 `libs/hdf-converters/sample_jsons/netsparker_mapper/sample_input_report/sample-netsparker-invicti.xml`, but different bytes,
so it was adapted. What changed is not recorded anywhere this check
could reach.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/netsparker-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.xml` — Invicti (Netsparker) Enterprise XML; the export carries no product version, so its version is not determinable from file content.
- `sample-netsparker-invicti.xml` — Invicti (Netsparker) Enterprise XML; the export carries no product version, so its version is not determinable from file content.

## Schema

No published schema for the Invicti (Netsparker) Enterprise XML export was located; the vendor documentation was not checked exhaustively (2026-09-20).
