# sarif-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `codeQL-output.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `codeql-extensions.sarif`
No counterpart in either upstream. Introduced by this repository in
`Fix/sarif security severity (#292)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `dockle_0_3_15.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `empty-results.sarif`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `eslint-10.9.1.sarif`
No counterpart in either upstream. Introduced by this repository in
`Ci/hdf enabled pipeline (#276)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `gitleaks_7.5.0.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `gosec.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `grype.sarif`
No counterpart in either upstream. Introduced by this repository in
`Fix/sarif security severity (#292)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `osv-scanner-2.5.1.sarif`
No counterpart in either upstream. Introduced by this repository in
`Ci/hdf enabled pipeline (#276)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `rich.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `sarif_input.sarif`
Byte-identical to SAF CLI `test/sample_data/sarif/sample_input_report/sarif_input.sarif`.

### `semgrep.sarif`
No counterpart in either upstream. Introduced by this repository in
`feat(converters): add semgrep-to-hdf converter (#237)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

### `spotbugs.sarif`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/sarif`, none matching.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/sarif-to-hdf/... -run TestSnapshots -update`.
