## The Vulnerabilities data model constrains on `tag=vulnerability tag=report`.
## Applying both to the hdf_finding eventtype makes failed/CVE findings populate
## the model (and light up Enterprise Security / risk-based alerting).

[eventtype=hdf_finding]
vulnerability = enabled
report = enabled
