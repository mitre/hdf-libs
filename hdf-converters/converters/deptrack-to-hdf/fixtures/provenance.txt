# deptrack-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `fpf-default.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/dependency_track_mapper/sample_input_report/fpf-default.json`.

### `fpf-info-vulnerability.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/dependency_track_mapper/sample_input_report/fpf-info-vulnerability.json`.

### `fpf-no-vulnerabilities.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/dependency_track_mapper/sample_input_report/fpf-no-vulnerabilities.json`.

### `fpf-optional-attributes.json`
Same filename as heimdall2 `libs/hdf-converters/sample_jsons/dependency_track_mapper/sample_input_report/fpf-optional-attributes.json`, but different bytes,
so it was adapted. What changed is not recorded anywhere this check
could reach.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/deptrack-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `fpf-default.json` — Dependency-Track Finding Packaging Format; `version`/`meta.version` 1.1.
- `fpf-info-vulnerability.json` — Dependency-Track Finding Packaging Format; `version`/`meta.version` 1.1.
- `fpf-no-vulnerabilities.json` — Dependency-Track Finding Packaging Format; `version`/`meta.version` 1.2.
- `fpf-optional-attributes.json` — Dependency-Track Finding Packaging Format; `version`/`meta.version` 4.10.1.

## Schema

Dependency-Track documents the Finding Packaging Format in prose only; the repository's `schema/` directory holds a vulnerability-policy schema, not FPF (2026-09-20).
