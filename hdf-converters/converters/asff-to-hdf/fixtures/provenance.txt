# asff-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty.json`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `inspec_rhel7_sample.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/asff_mapper/sample_input_report/rhel7_V-71931_asff.json`.

### `minimal.json`
No counterpart in either upstream checked. Introduced by this repository in
`Feat/asff to hdf (#147)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/asff`, none matching.

### `prowler_sample.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/asff_mapper/sample_input_report/prowler_sample.json`.

### `prowler_sample.ndjson`
Byte-identical to SAF CLI `test/sample_data/prowler/sample_input_report/prowler-sample.asff-json`.

### `securityhub_sample.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/asff_mapper/sample_input_report/asff_sample.json`.

### `trivy_config_secret_sample.json`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/1ckd trivy misconfig secret (#160)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/asff`, none matching.

### `trivy_cve_current_sample.json`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/1ckd trivy misconfig secret (#160)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/asff`, none matching.

### `trivy_sample.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/asff_mapper/sample_input_report/trivy-image_golang-1.12-alpine_sample.json`.

### `unknown-producer.json`
No counterpart in either upstream checked. Introduced by this repository in
`Feat/asff to hdf (#147)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/asff`, none matching.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/asff-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.json` — an empty `Findings` array; no producer, no version.
- `inspec_rhel7_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: arn:aws:securityhub:us-east-2:12345678910:ruleset/set/redhat-enterprise-linux-7-stig-baseline/rule/V-71931; the producing tool's own version is not carried by ASFF.
- `minimal.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: aws-foundational-security-best-practices/v/1.0.0/S3.2; the producing tool's own version is not carried by ASFF.
- `prowler_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: prowler-check11; the producing tool's own version is not carried by ASFF.
- `prowler_sample.ndjson` — NDJSON, one ASFF finding per line; same producers as the `.json` siblings; version not determinable from file content (ASFF carries no producer version).
- `securityhub_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: arn:aws:securityhub:::ruleset/cis-aws-foundations-benchmark/v/1.2.0/rule/1.1; the producing tool's own version is not carried by ASFF.
- `trivy_config_secret_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: Trivy/DS-0002; the producing tool's own version is not carried by ASFF.
- `trivy_cve_current_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: Trivy/CVE-2025-15467; the producing tool's own version is not carried by ASFF.
- `trivy_sample.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: Trivy; the producing tool's own version is not carried by ASFF.
- `unknown-producer.json` — ASFF `SchemaVersion` 2018-10-08;; producer per `GeneratorId`: AcmeScanner; the producing tool's own version is not carried by ASFF.

## Schema

ASFF has no JSON Schema. AWS publishes the Security Hub service model (Smithy, shape `AwsSecurityFinding`, in aws/aws-sdk-go-v2 `codegen/sdk-codegen/aws-models/securityhub.json`) and documents `SchemaVersion` 2018-10-08 as a constant. The `hdf-to-asff` exporter vendors a required-members extract of that model under `hdf-to-asff/schemas/` (2026-09-20).
