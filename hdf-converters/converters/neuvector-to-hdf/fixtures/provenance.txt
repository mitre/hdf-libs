# neuvector-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty.json`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `minimal.json`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/neuvector`, none matching.

### `neuvector-mitre-heimdall.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/neuvector_mapper/sample_input_report/neuvector-mitre-heimdall.json`.

### `neuvector-mitre-heimdall2.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/neuvector_mapper/sample_input_report/neuvector-mitre-heimdall2.json`.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/neuvector-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.json` — NeuVector scan report; `report.cvedb_version` 3.546 (the vulnerability database), while the scanner version is not in the file and so version not determinable from file content.
- `minimal.json` — NeuVector scan report; `report.cvedb_version` 3.546 (the vulnerability database), while the scanner version is not in the file and so version not determinable from file content.
- `neuvector-mitre-heimdall.json` — NeuVector scan report; `report.cvedb_version` 3.546 (the vulnerability database), while the scanner version is not in the file and so version not determinable from file content.
- `neuvector-mitre-heimdall2.json` — NeuVector scan report; `report.cvedb_version` 3.546 (the vulnerability database), while the scanner version is not in the file and so version not determinable from file content.

## Schema

The fixture is a REST response body; NeuVector publishes a Swagger 2.0 description of the controller API at `controller/api/apis.yaml` in neuvector/neuvector. It describes the endpoint, not a document format, and is not vendored (2026-09-20).
