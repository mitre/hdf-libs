# oscal-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `catalog-800-53-rev5.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
**Kept at full size on purpose (10 MB, over the ~1 MB fixture guideline; its golden is 1.8 MB).**
The 2026-09-14 fixture audit measured rather than assumed: the catalog holds 1196 control ids; two profile
fixtures resolve against it by control id and need 410 of them; and the converter tests assert its
completeness (`assert.Greater(len(Requirements), 1000)`, `assert.Len(Groups, 20)`). Trimming to the exact
profile closure would still leave roughly 4 MB while destroying the property under test — that this IS the
whole catalog. Shrinking it means weakening those assertions, so it stays.

### `catalog-moderate-resolved.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
**Kept at full size on purpose (3.8 MB, over the ~1 MB fixture guideline).** The converter test asserts
`assert.Equal(287, len(Requirements))` — 177 controls plus 110 enhancements — so the count is the property
under test and a subset cannot exercise it (2026-09-14 fixture audit).

### `component-definition-multi.json`
No counterpart in either upstream. Introduced by this repository in
`Fix/converter input fidelity (#330)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `component-example.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `poam-fedramp.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `profile-moderate.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `profile-redhat-fedramp-high.json`
No counterpart in either upstream. Introduced by this repository in
`Dev (#12)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `sap-fedramp.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `sar-fedramp.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `ssp-example.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

### `ssp-fedramp.json`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/oscal-to-hdf/... -run TestSnapshots -update`.
