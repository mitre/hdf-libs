# msft-secure-score-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `combined.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/msft_secure_score_mapper/sample_input_report/combined.json`.

### `minimal.json`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/msft-secure-score-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `combined.json` — Microsoft Graph secureScores response body; the service version is not part of the response, so its version is not determinable from file content.
- `minimal.json` — Microsoft Graph secureScores response body; the service version is not part of the response, so its version is not determinable from file content.

## Schema

The fixture is a Microsoft Graph `security/secureScores` response; the Graph OpenAPI description (microsoftgraph/msgraph-metadata, `openapi/v1.0/openapi.yaml`, tens of MB) covers it and is not vendored (2026-09-20).
