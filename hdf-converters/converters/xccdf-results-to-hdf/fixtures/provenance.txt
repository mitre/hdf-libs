# xccdf-results-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `arf-minimal.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-minimal-1.1.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-minimal-1.2.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-ssg-nested-groups.xml`
No counterpart in either upstream. Introduced by this repository in
`fix(xccdf): traverse nested Groups, keep every rule, and map severity (#137)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `empty.xml`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `minimal.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `stig-rhel7.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `xccdf-results-openscap-rhel7.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-openscap-rhel7.xml`.

### `xccdf-results-openscap-rhel8.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-openscap-rhel8.xml`.

### `xccdf-results-scc-rhel7.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-scc-rhel7.xml`.

### `xccdf-results-scc-rhel8.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-scc-rhel8.xml`.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/xccdf-results-to-hdf/... -run TestSnapshots -update`.
