# xccdf-results-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `arf-minimal.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-minimal-1.1.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-minimal-1.2.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `benchmark-ssg-nested-groups.xml`
No counterpart in either upstream. Introduced by this repository in
`fix(xccdf): traverse nested Groups, keep every rule, and map severity (#137)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `empty.xml`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `minimal.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `stig-rhel7.xml`
No counterpart in either upstream. Introduced by this repository in
`Development (#10)`; no upstream source is recorded, so it was
most likely authored here for that change.
SAF does carry samples for this tool under `test/sample_data/xccdf_results`, none matching.

### `xccdf-results-openscap-rhel7.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-openscap-rhel7.xml`.

### `xccdf-results-openscap-rhel8.xml`
Derived from heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-openscap-rhel8.xml`
(an OpenSCAP 1.2.17 run of the DISA RHEL 8 STIG, release 1.3, 248 rule-results), of which this file was a byte-identical copy until 2026-09-19.
Trimmed then to 16 rule-results: the first two in document order for each distinct (result, severity) pair the run contains — the one `error`/`low`
and one `error`/`high` are all the run had — plus `SV-230221`, which the converter tests pin by id. Only the Groups and Rules those rule-results
reference are kept, and the Profile `select` entries for the removed Groups are dropped so the document stays consistent; the Benchmark metadata,
TestResult header, target facts, and score are untouched. Every distinct output shape the full file produced is still produced, and the kept
requirements are byte-identical to their pre-trim golden entries; only the volume changed. Text was removed, never edited, so the retained
content is still verbatim upstream. The full file is in this repository's history before that date if a volume test ever needs it.

### `xccdf-results-scc-rhel7.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-scc-rhel7.xml`.

### `xccdf-results-scc-rhel8.xml`
Derived from heimdall2 `libs/hdf-converters/sample_jsons/xccdf_results_mapper/sample_input_report/xccdf-results-scc-rhel8.xml`
(an SCC 5.4.2 run of the DISA RHEL 8 STIG, 248 rule-results), of which this file was a byte-identical copy until 2026-09-19.
Trimmed then to 12 rule-results: the first two in document order for each of the six distinct (result, severity) pairs the run contains, plus
`SV-230221` and the rule-result timed `2021-12-17T10:24:35`, both of which the converter tests pin. Only the Groups and Rules those rule-results
reference are kept, and the Profile `select` entries for the removed Groups are dropped so the document stays consistent; the Benchmark metadata,
TestResult header, target facts, and scores are untouched. Every distinct output shape the full file produced is still produced, and the kept
requirements are byte-identical to their pre-trim golden entries; only the volume changed. Text was removed, never edited, so the retained
content is still verbatim upstream. The full file is in this repository's history before that date if a volume test ever needs it.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/xccdf-results-to-hdf/... -run TestSnapshots -update`.
