# veracode-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty.xml`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `veracode.xml`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/veracode_mapper/sample_input_report/veracode.xml`.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/veracode-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.xml` — Veracode `detailedreport` with no `report_format_version` attribute (an empty-report fixture); version not determinable from file content.
- `veracode.xml` — Veracode 1.5 (`detailedreport/@report_format_version`).

## Schema

Veracode publishes the XSD for the detailed report at `https://analysiscenter.veracode.com/resource/detailedreport.xsd` (target namespace `https://www.veracode.com/schema/reports/export/1.0`; `veracode.xml` declares `report_format_version` 1.5). It is vendored here as `detailedreport.xsd` and recorded in `provenance.json`; the Go sweep hash-checks it and no test compiles it.
