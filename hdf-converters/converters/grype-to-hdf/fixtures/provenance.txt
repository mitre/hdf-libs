# grype-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `amazon.json`
Same filename as heimdall2 `libs/hdf-converters/sample_jsons/anchore_grype_mapper/sample_input_report/amazon.json`, but different bytes,
so it was adapted. What changed is not recorded anywhere this check
could reach.

### `anchore_grype.json`
Same filename as heimdall2 `libs/hdf-converters/sample_jsons/anchore_grype_mapper/sample_input_report/anchore_grype.json`, but different bytes,
so it was adapted. What changed is not recorded anywhere this check
could reach.

### `empty.json`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `tensorflow.json`
Same filename as heimdall2 `libs/hdf-converters/sample_jsons/anchore_grype_mapper/sample_input_report/tensorflow.json`, but different bytes,
so it was adapted. What changed is not recorded anywhere this check
could reach.

### `directory_scan.json`
Real grype 0.117.0 output from an actual `grype dir:. -o json` run
(2026-09-20) over a one-line `requirements.txt` pinning `Jinja2==2.10`.
Captured to cover the directory-scan shape the four image fixtures never
exercised: `source.type` is `directory` and `source.target` is the bare
string `"."` (not the image object) — the shape that used to hard-fail the
Go converter (hdf-libs-yt05). The scan was run from inside the target dir so
every path is relative; the only machine-local values, grype's DB cache-dir
under `descriptor.configuration.db.cache-dir` and `descriptor.db.status.path`,
were replaced with `/grype-cache/db` (converter-irrelevant metadata). No
other bytes were altered.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/grype-to-hdf/... -run TestSnapshots -update`.
