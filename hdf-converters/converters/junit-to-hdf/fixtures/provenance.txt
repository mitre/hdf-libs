# junit-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `checkov-junit.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/checkov nist mapping and junit warning (#344)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `empty.xml`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `node-test-hybrid.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/converter input fidelity (#330)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `node-test-mixed.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/junit testsuite less (#293)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `node-test-nested.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/converter input fidelity (#330)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `node-test-passing.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Fix/junit testsuite less (#293)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `surefire-error.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `surefire-failing.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `surefire-flaky.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `testsuites-mixed.xml`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/junit-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `checkov-junit.xml` — JUnit XML (`<testsuites disabled="0" errors="0" failures="10" tests="18" time="0.0">`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `empty.xml` — JUnit XML (`<testsuites name="EmptySuite">`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `node-test-hybrid.xml` — JUnit XML (`<testsuites>`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `node-test-mixed.xml` — JUnit XML (`<testsuites>`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `node-test-nested.xml` — JUnit XML (`<testsuites>`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `node-test-passing.xml` — JUnit XML (`<testsuites>`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `surefire-error.xml` — JUnit XML (`<testsuite name="surefire.MyTest" time="0" tests="1" errors="1" skipped="0" fail`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `surefire-failing.xml` — JUnit XML (`<testsuite failures="2" time="0.014" errors="0" skipped="0" tests="2" name="org.`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `surefire-flaky.xml` — JUnit XML (`<testsuite version="3.0.2" name="org.acme.FlakyTest" time="1.324" tests="1" erro`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.
- `testsuites-mixed.xml` — JUnit XML (`<testsuites>`); the producing framework writes no version into JUnit XML, so its version is not determinable from file content.

## Schema

JUnit XML has no official schema: the format is defined by what the Ant/Maven/Jenkins tooling emits. Community XSDs exist (windyroad/JUnit-Schema `JUnit.xsd`; the Jenkins `junit-4.xsd` the JUnit 5 project uses for its own tests) and disagree with each other; none is vendored, because none is authoritative for the producers these fixtures come from (2026-09-20).
