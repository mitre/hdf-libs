# nikto-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty-vulns.json`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `minimal.json`
No counterpart in either upstream checked. Introduced by this repository in
`Development (#10)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.
SAF does carry samples for this tool under `test/sample_data/nikto`, none matching.

### `zero.webappsecurity.json`
Byte-identical to heimdall2 `libs/hdf-converters/sample_jsons/nikto_mapper/sample_input_report/zero.webappsecurity.json`.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/nikto-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty-vulns.json` — Nikto JSON; the file carries only the target `banner` (nginx/1.24.0), no Nikto version, so its version is not determinable from file content.
- `minimal.json` — Nikto JSON; the file carries only the target `banner` (Apache/2.4.41), no Nikto version, so its version is not determinable from file content.
- `zero.webappsecurity.json` — Nikto JSON; the file carries only the target `banner` (Apache/2.2.6 (Win32) mod_ssl/2.2.6 OpenSSL/0.9.8e mod_jk/1.2.40), no Nikto version, so its version is not determinable from file content.

## Schema

Nikto publishes no schema for its JSON report; it is hand-emitted by `program/plugins/nikto_report_json.plugin` (2026-09-20).
