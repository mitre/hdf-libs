# semgrep-to-hdf fixtures — provenance

Recorded 2026-09-14. Correspondence was established by content hash against
mitre/heimdall2 at 68d88f3c5 (2026-06-16) and mitre/saf at 8037c390 (2026-06-29).
Those are the only upstreams checked; a fixture matching neither may still
have come from somewhere else.

## `input/`

### `empty.json`
Synthetic by construction: an empty or no-findings document used to
exercise the nothing-to-convert path. Not sourced from anywhere.

### `errors.json`
No counterpart in either upstream checked. Introduced by this repository in
`feat(converters): add semgrep-to-hdf converter (#237)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `minimal.json`
No counterpart in either upstream checked. Introduced by this repository in
`feat(converters): add semgrep-to-hdf converter (#237)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

### `real.json`
No counterpart in either upstream checked. Introduced by this repository in
`feat(converters): add semgrep-to-hdf converter (#237)`, a commit that records no source. Origin not established by those searches;
what the file says about its own producer is in the "Tool and version" section below.

## `expected/`

Generated by this repository's converters, not sourced. Regenerate with
`go test ./converters/semgrep-to-hdf/... -run TestSnapshots -update`.

## Tool and version, read from the file itself (2026-09-20)

A version below is what the fixture *says about itself*; where a format carries no producer version, that is stated rather than guessed.

- `empty.json` — Semgrep 1.174.0 (`version`).
- `errors.json` — Semgrep 1.174.0 (`version`).
- `minimal.json` — Semgrep 1.174.0 (`version`).
- `real.json` — Semgrep 1.174.0 (`version`).

## Schema

Semgrep publishes the JSON Schema for `--json` output in semgrep/semgrep-interfaces, tagged per release. The fixtures report `version` 1.174.0, so that tag's `semgrep_output_v1.jsonschema` is vendored here as `semgrep_output_v1-1.174.0.schema.json` and recorded in `provenance.json`.

Validation (gojsonschema, (2026-09-20)): `empty.json`, `minimal.json` and `real.json` validate. `errors.json` does not — its `errors[0].type` matches none of the schema's error-type variants; that file was authored here to exercise the error path (see above), so the fixture policy's validation requirement applies and is not met. Recorded as a defect, not corrected here.
