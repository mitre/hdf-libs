{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mitre.github.io/hdf-libs/schemas/primitives/bom/v3.5.0",
  "title": "HDF BOM Primitives",
  "description": "Generalized Bill-of-Materials / manifest representation. One extensible Bom shape, discriminated by bomType, carries any manifest kind (SBOM, AI model/dataset, and reserved future kinds) either by passthrough (opaque ref/embed of the native document) or normalized (converted into HDF's queryable shape). Subject identity (name, version, componentId) is inherited from the host component or system — the BOM never re-owns it. See ADR-0001.",
  "$defs": {
    "Bom_Type": {
      "type": "string",
      "title": "BOM Type",
      "anyOf": [
        {
          "enum": [
            "sbom",
            "ai-model",
            "dataset",
            "hbom",
            "cbom",
            "saasbom",
            "obom",
            "mbom",
            "kbom"
          ]
        },
        { "pattern": "^x-[a-z0-9]+(-[a-z0-9]+)*$" }
      ],
      "description": "Discriminator for the manifest kind. Reserved, CycloneDX-aligned set: sbom (software), ai-model, dataset, hbom (hardware), cbom (cryptography), saasbom (services), obom (operations), mbom (manufacturing), kbom (Kubernetes). Normalized now: sbom, ai-model, dataset; reserved + passthrough-capable now, normalized in later releases: the rest. Custom or experimental kinds MUST use an 'x-' prefix (e.g. 'x-threat-model') so they cannot collide with a value later promoted into the reserved set; a non-'x-' value outside the reserved set is invalid. Adding a reserved value is a schema version bump. VEX/VDR/SecurityProfile are vulnerability assertions, not BOMs, and are intentionally excluded."
    },
    "Bom": {
      "type": "object",
      "title": "Bill of Materials",
      "additionalProperties": false,
      "required": [
        "bomType",
        "format"
      ],
      "properties": {
        "bomType": {
          "$ref": "#/$defs/Bom_Type",
          "description": "The manifest kind. Determines which normalized type-extension (model/dataset/packages) may appear."
        },
        "format": {
          "type": "string",
          "description": "Source manifest format the BOM was produced from or references. Examples: cyclonedx, cyclonedx-ml, spdx, spdx-ai, huggingface, croissant. Free-form so new formats need no schema change; the converter that emits the BOM owns the value."
        },
        "ref": {
          "type": "string",
          "format": "uri-reference",
          "description": "Passthrough by reference: URI (relative path, absolute URI, or fragment) to the native manifest document. Present for externally-hosted BOMs."
        },
        "document": {
          "type": "object",
          "description": "Passthrough by embedding: the native manifest carried opaquely (e.g. a raw CycloneDX or SPDX object). HDF does not constrain its internal shape — full manifest validation is a tool-level concern."
        },
        "hashes": {
          "type": "array",
          "items": {
            "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v3.5.0#/$defs/Checksum"
          },
          "description": "Integrity of the carried BOM *document* itself (the referenced/embedded manifest file), NOT the integrity of the BOM's subject artifact. Subject/artifact integrity (model weights, dataset archive) is a component-level concern, not carried here. Subject identity is inherited from the host component/system; per-node identity lives in the type-extension."
        },
        "uniqueId": {
          "type": "string",
          "description": "Optional stable identifier for this BOM document (e.g. CycloneDX serialNumber, SPDX documentNamespace). Correlates the same BOM across evidence packages."
        },
        "license": {
          "type": ["string", "null"],
          "description": "Optional license of the BOM document as a whole (SPDX license expression). Nullable and often meaningless (CBOM algorithms, many HBOM parts have no license); per-node licenses live in the type-extension, not here."
        },
        "model": {
          "$ref": "#/$defs/AI_Model_Extension",
          "description": "Normalized ai-model extension. Permitted only when bomType is ai-model."
        },
        "dataset": {
          "$ref": "#/$defs/Dataset_Extension",
          "description": "Normalized dataset extension. Permitted only when bomType is dataset."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SBOM_Package"
          },
          "description": "Normalized sbom extension: the flattened software package inventory. Permitted only when bomType is sbom."
        }
      },
      "allOf": [
        {
          "$comment": "Three-tier discipline: the ai-model extension may appear only on an ai-model BOM.",
          "if": {
            "properties": { "bomType": { "const": "ai-model" } },
            "required": ["bomType"]
          },
          "else": {
            "properties": { "model": false }
          }
        },
        {
          "$comment": "The dataset extension may appear only on a dataset BOM.",
          "if": {
            "properties": { "bomType": { "const": "dataset" } },
            "required": ["bomType"]
          },
          "else": {
            "properties": { "dataset": false }
          }
        },
        {
          "$comment": "The normalized packages extension may appear only on an sbom BOM.",
          "if": {
            "properties": { "bomType": { "const": "sbom" } },
            "required": ["bomType"]
          },
          "else": {
            "properties": { "packages": false }
          }
        },
        {
          "$comment": "A BOM must carry at least one of ref/document/packages/model/dataset. This intentionally checks PRESENCE, not non-emptiness: under the partial-fidelity design a converter MAY emit an empty normalized extension (e.g. model:{}) alongside a raw `document` passthrough when a source has no cleanly-mappable fields, so this constraint deliberately does not require minProperties/minItems on the extension. A bare {bomType, format} carrying none of these keys is unusable and invalid.",
          "anyOf": [
            { "required": ["ref"] },
            { "required": ["document"] },
            { "required": ["packages"] },
            { "required": ["model"] },
            { "required": ["dataset"] }
          ]
        }
      ],
      "examples": [
        {
          "$comment": "Passthrough SBOM by external reference — the universal escape hatch, available to every bomType.",
          "bomType": "sbom",
          "format": "cyclonedx",
          "ref": "https://artifacts.agency.gov/sbom/webtier.cdx.json",
          "hashes": [
            { "algorithm": "sha256", "value": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855" }
          ]
        },
        {
          "$comment": "Passthrough for a reserved-but-not-yet-normalized type (cbom) — carried opaquely until a normalized extension ships.",
          "bomType": "cbom",
          "format": "cyclonedx",
          "document": { "bomFormat": "CycloneDX", "specVersion": "1.6" }
        },
        {
          "$comment": "Normalized ai-model BOM: CISA/G7-aligned fields live in the model extension, never at the component root or shared base.",
          "bomType": "ai-model",
          "format": "cyclonedx-ml",
          "model": {
            "modelArchitecture": "transformer",
            "parameterCount": 6738415616,
            "serializationFormat": "safetensors",
            "adaptationType": "finetune",
            "baseModelRef": "meta-llama/Llama-2-7b-hf",
            "learningApproach": "supervised",
            "task": "text-generation",
            "performanceMetrics": [
              { "name": "MMLU", "value": "0.457" }
            ],
            "hyperparameters": [
              { "name": "epochs", "value": "3" },
              { "name": "learning-rate", "value": "2e-5" }
            ],
            "inputOutput": {
              "dataTypes": ["string"],
              "modality": ["text"],
              "contextLength": 4096,
              "tokenizer": "llama-bpe"
            }
          }
        },
        {
          "$comment": "Normalized dataset BOM with lineage — a filtered derivative of a base dataset, referenced by an ai-model via datasetRefs.",
          "bomType": "dataset",
          "format": "croissant",
          "dataset": {
            "recordCount": 2500000,
            "datasetFormat": "parquet",
            "dataClassification": "public",
            "intendedUse": "instruction fine-tuning",
            "modality": "text",
            "provenance": "Filtered from the public UltraChat corpus; low-quality and duplicate turns removed.",
            "statisticalProperties": "Mean 512 tokens/record; 61% single-turn, 39% multi-turn; English-dominant (~92%).",
            "baseDatasetRefs": ["HuggingFaceH4/ultrachat_200k"],
            "derivation": "filtered"
          }
        }
      ]
    },
    "AI_Model_Extension": {
      "type": "object",
      "additionalProperties": true,
      "description": "Normalized AI-model fields, aligned to the CISA/G7 'SBOM for AI' minimum elements. All fields optional (standards-correct; only the EU AI Act makes a subset binding for high-risk/GPAI). Left open (additionalProperties: true) so a converter can carry unmapped native fields opaquely (partial-fidelity pattern). Subject name/version are inherited from the host aiModel component, not repeated here.",
      "title": "AI Model BOM Extension",
      "properties": {
        "modelArchitecture": {
          "type": "string",
          "description": "Model architecture family. Examples: transformer, cnn, diffusion, mixture-of-experts."
        },
        "parameterCount": {
          "type": "integer",
          "minimum": 0,
          "description": "Total trainable parameter count. No native CycloneDX/SPDX field exists; first-class here because the EU AI Act keys GPAI obligations off model scale."
        },
        "serializationFormat": {
          "type": "string",
          "description": "On-disk weight serialization format. Security-critical yet under-modeled by BOM specs: pickle/pytorch permits arbitrary code execution on load; safetensors does not. Examples: safetensors, pytorch, gguf, onnx, tensorflow."
        },
        "adaptationType": {
          "type": "string",
          "title": "Model Adaptation Type",
          "enum": ["finetune", "adapter", "quantized", "merge"],
          "description": "Lineage relationship to baseModelRef, adopting Hugging Face's base_model_relation vocabulary (the only typed lineage enum in the ecosystem)."
        },
        "baseModelRef": {
          "type": "string",
          "description": "Reference to the base model this one was adapted from (e.g. a Hugging Face repo id or purl). Correlates the lineage edge; the base model itself may carry its own ai-model BOM."
        },
        "datasetRefs": {
          "type": "array",
          "items": { "type": "string" },
          "description": "References to the training/evaluation datasets this model was produced from — preferably a dataset component's componentId (correlating to a first-class dataset subject rather than duplicating dataset detail, per ADR §10), or a dataset BOM uniqueId/URI when no component exists."
        },
        "intendedUse": {
          "type": "string",
          "description": "Free-text statement of intended use and out-of-scope uses (CISA/G7 minimum element)."
        },
        "learningApproach": {
          "type": "string",
          "description": "Model's learning/training paradigm. Free-text because the value set varies across standards. Examples: supervised, self-supervised, semi-supervised, reinforcement. Cross-standard intersection: SPDX 3.0 ai_typeOfModel ∩ CycloneDX modelParameters.approach.type ∩ CISA/G7 learning type."
        },
        "task": {
          "type": "string",
          "description": "The ML task the model performs. Free-text. Examples: text-classification, sentiment-analysis, object-detection, translation. Sourced from CycloneDX modelParameters.task (SPDX 3.0 ai_domain is domain-level/adjacent, not the task); CISA/G7 intended-application."
        },
        "performanceMetrics": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "name": {
                "type": "string",
                "description": "Metric name. Examples: accuracy, f1, BLEU, latency."
              },
              "value": {
                "type": "string",
                "description": "Reported metric value, free-text because metrics are heterogeneous (percentages, scores, milliseconds)."
              }
            }
          },
          "description": "Reported evaluation metrics as name/value pairs. Values are free-text because metrics are heterogeneous (accuracy, f1, BLEU, latency). The item stays open (additionalProperties: true) to carry native CycloneDX slice/confidenceInterval detail opaquely. Cross-standard intersection: SPDX 3.0 ai_metric ∩ CycloneDX quantitativeAnalysis.performanceMetrics ∩ CISA/G7 KPI cluster."
        },
        "hyperparameters": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Hyper-parameter name. Examples: epochs, learning-rate, batch-size."
              },
              "value": {
                "type": "string",
                "description": "Hyper-parameter value (free-text)."
              }
            }
          },
          "description": "Training hyper-parameters as name/value pairs (epochs, learning-rate, batch-size). This is the set of training knobs, NOT the model's trainable parameterCount — the two are routinely conflated. CISA/G7 'Model properties: hyper-parameters'; SPDX 3.0 ai_hyperparameter."
        },
        "inputOutput": {
          "type": "object",
          "additionalProperties": true,
          "description": "Model input/output properties (CISA/G7 minimum element): I/O data types, modality, context/sequence length, and tokenizer. Sourced from CycloneDX modelParameters.inputs[].format / outputs[].format. All sub-fields optional.",
          "properties": {
            "dataTypes": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Input/output data types. Examples: string, byte[], float32, image."
            },
            "modality": {
              "type": ["string", "array"],
              "items": { "type": "string" },
              "description": "Input/output modalities. String or array of strings. Examples: text, image, audio. Symmetric with Dataset_Extension.modality."
            },
            "contextLength": {
              "type": "integer",
              "minimum": 0,
              "description": "Maximum context/sequence length the model accepts, in tokens."
            },
            "tokenizer": {
              "type": "string",
              "description": "Tokenizer used for the model's input encoding. Examples: BPE, SentencePiece, tiktoken."
            }
          }
        }
      }
    },
    "Dataset_Extension": {
      "type": "object",
      "additionalProperties": true,
      "description": "Normalized dataset fields (SPDX 3.0 Dataset profile / MLCommons Croissant aligned). All optional; open for partial-fidelity passthrough of unmapped native fields. Symmetric with AI_Model_Extension: carries the dataset's own lineage (baseDatasetRefs/derivation) just as the model extension carries baseModelRef/adaptationType.",
      "title": "Dataset BOM Extension",
      "properties": {
        "recordCount": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of records/examples in the dataset."
        },
        "datasetFormat": {
          "type": "string",
          "description": "Physical format of the dataset. Examples: parquet, csv, jsonl, tfrecord, webdataset."
        },
        "dataClassification": {
          "type": "string",
          "description": "Sensitivity/classification of the data. Examples: public, internal, confidential, pii, phi."
        },
        "intendedUse": {
          "type": "string",
          "description": "Free-text statement of the dataset's intended use (CISA/G7 minimum element)."
        },
        "modality": {
          "type": ["string", "array"],
          "items": { "type": "string" },
          "description": "Content modality/kind of the data (CISA/G7 'Dataset content: modality'). Examples: text, image, tabular, timeseries, audio. DISTINCT from datasetFormat, which is the physical encoding (parquet/csv). Resolves SPDX 3.0 dataset_datasetType, which is content-kind, not physical format. String or array of strings."
        },
        "provenance": {
          "type": "string",
          "description": "Free-text description of the dataset's origin and collection process (CISA/G7 'Dataset provenance'; SPDX 3.0 dataset_dataCollectionProcess)."
        },
        "statisticalProperties": {
          "type": "string",
          "description": "Free-text summary of the dataset's statistical properties (CISA/G7 minimum element; open-ended by design). Intentionally free-text and will remain so permanently: recordCount is the one structured statistic, and restructuring this into an object later would break existing consumers, so it stays a string (additive-only)."
        },
        "baseDatasetRefs": {
          "type": "array",
          "items": { "type": "string" },
          "description": "References to the source dataset(s) this one was derived from — a dataset componentId, or a dataset BOM uniqueId/URI when no component exists. The lineage edge parallel to ai-model's baseModelRef; the base dataset may carry its own dataset BOM."
        },
        "derivation": {
          "type": "string",
          "title": "Dataset Derivation Type",
          "enum": ["filtered", "augmented", "merged", "sampled"],
          "description": "Relationship of this dataset to baseDatasetRefs, parallel to the model extension's adaptationType. Minimal + extensible: filtered (subset by rule), augmented (added/synthesized records), merged (union of sources), sampled (statistical draw)."
        }
      }
    },
    "SBOM_Package": {
      "type": "object",
      "additionalProperties": true,
      "description": "A single normalized software package entry within an sbom BOM. Minimal identity + version for querying/diffing; the full native record remains available via passthrough (document/ref).",
      "title": "SBOM Package",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Package name."
        },
        "version": {
          "type": "string",
          "description": "Package version."
        },
        "purl": {
          "type": "string",
          "description": "Package URL (purl) — the preferred cross-BOM identity key when present."
        },
        "licenses": {
          "type": "array",
          "items": { "type": "string" },
          "description": "SPDX license identifiers/expressions for this package."
        }
      }
    }
  }
}
