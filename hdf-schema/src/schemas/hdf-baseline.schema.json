{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mitre.github.io/hdf-libs/schemas/hdf-baseline/v1.0.0",
  "type": "object",
  "additionalProperties": true,
  "required": ["name", "supports", "requirements", "groups", "checksum"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The name - must be unique."
    },
    "title": {
      "type": ["string", "null"],
      "description": "The title - should be human readable."
    },
    "maintainer": {
      "type": ["string", "null"],
      "description": "The maintainer(s)."
    },
    "copyright": {
      "type": ["string", "null"],
      "description": "The copyright holder(s)."
    },
    "copyrightEmail": {
      "type": ["string", "null"],
      "description": "The email address or other contact information of the copyright holder(s)."
    },
    "license": {
      "type": ["string", "null"],
      "description": "The copyright license. Example: 'Apache-2.0'."
    },
    "summary": {
      "type": ["string", "null"],
      "description": "The summary. Example: the Security Technical Implementation Guide (STIG) header."
    },
    "version": {
      "type": ["string", "null"],
      "description": "The version of the baseline."
    },
    "supports": {
      "type": "array",
      "items": {
        "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Supported_Platform"
      },
      "description": "The set of supported platform targets."
    },
    "requirements": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Baseline_Requirement"
      },
      "description": "The set of requirements - contains no findings as the assessment has not yet occurred."
    },
    "groups": {
      "type": "array",
      "items": {
        "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Requirement_Group"
      },
      "description": "A set of descriptions for the requirement groups."
    },
    "inputs": {
      "type": ["array", "null"],
      "items": {
        "type": "object",
        "additionalProperties": true,
        "description": "An input or attribute used in the run."
      },
      "description": "The input(s) or attribute(s) to be used in the run."
    },
    "checksum": {
      "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Checksum",
      "description": "Cryptographic checksum for baseline integrity verification."
    },
    "status": {
      "type": ["string", "null"],
      "description": "The status. Example: 'loaded'."
    },
    "depends": {
      "type": ["array", "null"],
      "items": {
        "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Dependency"
      },
      "description": "The set of dependencies this baseline depends on."
    },
    "generator": {
      "anyOf": [
        { "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/extensions/v1.0.0#/definitions/Generator" },
        { "type": "null" }
      ],
      "description": "The tool that generated this file."
    }
  },
  "description": "Information on the set of requirements that can be assessed. Example: it can include the name of the InSpec profile.",
  "title": "HDF Baseline",
  "definitions": {
    "Baseline_Requirement": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id", "impact", "tags", "code", "descriptions"],
      "properties": {
        "id": {
          "type": "string",
          "description": "The requirement identifier. Example: 'SV-238196'."
        },
        "title": {
          "type": ["null", "string"],
          "description": "The title - is nullable."
        },
        "descriptions": {
          "$ref": "#/definitions/Baseline_Requirement_Descriptions",
          "description": "Array of labeled descriptions. At least one description with label 'default' must be present. Convention: place default description first. Common labels: 'default', 'check', 'fix', 'rationale'."
        },
        "impact": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "The impactfulness or severity (0.0 to 1.0)."
        },
        "severity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low", "informational"],
          "description": "Explicit severity rating. Typically derived from impact score but provided explicitly for clarity."
        },
        "refs": {
          "type": ["array", "null"],
          "items": {
            "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Reference"
          },
          "description": "The set of references to external documents."
        },
        "tags": {
          "type": "object",
          "additionalProperties": true,
          "description": "A set of tags - usually metadata like CCI, STIG ID, severity."
        },
        "code": {
          "type": "string",
          "description": "The raw source code of the requirement. Note that if this is an overlay, it does not include the underlying source code."
        },
        "sourceLocation": {
          "anyOf": [
            { "$ref": "https://mitre.github.io/hdf-libs/schemas/primitives/common/v1.0.0#/definitions/Source_Location" },
            { "type": "null" }
          ],
          "description": "The explicit location of the requirement within the source code."
        }
      },
      "description": "A requirement definition without assessment results.",
      "title": "Baseline Requirement"
    },
    "Baseline_Requirement_Descriptions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["label", "data"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Description category. The 'default' label is required for the primary description. Common labels: 'default', 'check', 'fix', 'rationale'. Tools may use custom labels."
          },
          "data": {
            "type": "string",
            "description": "The description text content."
          }
        }
      },
      "description": "Array of labeled descriptions. At least one description with label 'default' must be present.",
      "title": "Baseline Requirement Descriptions"
    }
  }
}
