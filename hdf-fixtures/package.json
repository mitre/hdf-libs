{
  "name": "@mitre/hdf-fixtures",
  "version": "3.6.0",
  "description": "Shared real-world HDF reference fixtures for cross-package consumers (parsers, validators, extension-graph)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "results",
    "baseline",
    "inspec"
  ],
  "scripts": {
    "build": "tsdown",
    "clean": "rimraf dist",
    "test": "echo 'No TypeScript tests in hdf-fixtures (Go gate runs via test:go)' && exit 0",
    "test:ts": "echo 'No TypeScript tests in hdf-fixtures (Go gate runs via test:go)' && exit 0",
    "type-check": "tsc --noEmit",
    "lint": "eslint src",
    "test:go": "go test ./..."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitre/hdf-libs.git",
    "directory": "hdf-fixtures"
  },
  "author": "MITRE Corporation",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
